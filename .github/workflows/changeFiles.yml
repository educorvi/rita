# This workflow is disabled as it was specific to Rush's change tracking system
# With Yarn workspaces, version management is handled differently

# name: Verify change files

# on:
#     pull_request:
#         branches: ['main', 'develop']

# jobs:
#     build:
#         runs-on: ubuntu-latest

#         steps:
#             - uses: actions/checkout@v3
#               with:
#                   fetch-depth: 0
#             - name: Use Node.js
#               uses: actions/setup-node@v3
#               with:
#                   node-version: 22.x
#             - name: Check for changes files
#               run: echo "Rush change tracking disabled"
