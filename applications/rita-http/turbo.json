{
    "extends": ["//"],
    "tasks": {
        "build:internal": {
          "dependsOn": ["build:routes", "^build:internal"]
        },
        "check-types": {
          "dependsOn": ["build:routes", "^check-types"]
        },
        "build:routes": {
          "outputs": ["generated/**", "^build:internal"]
        }
    }
}
